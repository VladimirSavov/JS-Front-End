function matrix(input) {
    for (let i = 0; i < input; i++) {
        let row = '';
        for (let k = 0; k < input; k++) {
            row += input + ' ';
            
        }
        console.log(row);
        
    }
}

matrix(3);